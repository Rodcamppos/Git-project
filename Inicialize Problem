def generate(numRows):
    triangle = []
    for i in range(numRows): #Neste loop, i representa o numero da linha atual, e gera as linhas do triangulo
        row = [1] * (i + 1)  #cria linhas com 1's
        for j in range(1, i): #neste loop, somamos os 1's no meio da lista, exceto os das extremidades
           


   
